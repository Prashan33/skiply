# Sanity Studio (standalone workspace)
# Copy to studio/.env and fill in. These use the SANITY_STUDIO_ prefix so the
# Studio build can inline them — they are NOT secrets.
SANITY_STUDIO_PROJECT_ID="g178ibto"
SANITY_STUDIO_DATASET="production"
SANITY_STUDIO_API_VERSION="2026-08-29"
