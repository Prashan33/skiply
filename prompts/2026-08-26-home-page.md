Date: 2026-08-26

## Prompt

> implement vertex home page from design, remember our project name is akiply ont vertex

## Result

Implemented the homepage from `design/vertex-home.png`, rebranded from
Vertex to Skiply.

- Homepage: `app/page.tsx`
- Extended `TopNav` (`components/ui/Navigation.tsx`) with a notifications
  bell and avatar
- Extended `CourseCard` (`components/ui/Card.tsx`) with an `iconClassName`
  prop for per-card icon color
- Moved the earlier design-system showcase from `app/page.tsx` to
  `app/design-system/page.tsx` so it no longer occupies the home route
